package ${PACKAGE_NAME};

import android.content.Context
import android.content.Intent

class ${NAME}:  BaseActivity() {

    override fun init() {}

    override fun withToolBar() = false

    companion object {
        fun callingIntent(context: Context) = Intent(context, ${NAME}::class.java)
    }

    override fun fragment() = ${FRAGMENT_NAME}()

}